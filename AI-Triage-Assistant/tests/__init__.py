"""
Test suite for the AI Triage Assistant.

This package contains unit and integration tests for:
- Email sending (email_sender.py)
- Configuration loading (config.py)
- Triage pipeline (triage.py)
- Streamlit UI (streamlit_app.py)

All tests include security checks to ensure API keys and secrets
are never leaked in error messages or logs.
"""
